# TVMaze API

TV show information.

**Base URL:** `https://api.tvmaze.com`

**Auth:** None required

**Status:** ✅ Verified

## Endpoints

### GET /search/shows
Search for shows
- **Query:** `q` (search term)

### GET /shows/{id}
Get show by ID

### GET /shows/{id}/episodes
Get episodes for a show
